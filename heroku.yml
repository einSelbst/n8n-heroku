setup:
    addons:
      - plan: mongolab
        as: DATABASE

    config:
        N8N_BASIC_AUTH_ACTIVE: true
        N8N_BASIC_AUTH_USER: kollect-admin
        N8N_BASIC_AUTH_PASSWORD: REPLACE_ME
        N8N_VERSION: 0.74.0

build:
    docker:
        web: Dockerfile